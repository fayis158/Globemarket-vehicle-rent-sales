import 'package:flutter/material.dart';


class Vehicle_Removed extends StatelessWidget {
  const Vehicle_Removed({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(""),
      ),
    );
  }
}